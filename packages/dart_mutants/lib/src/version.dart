/// This package's own version, recorded in every run's statistics so runs
/// from different releases can be told apart. Kept equal to `pubspec.yaml`'s
/// by a test.
const String packageVersion = '0.2.9';
