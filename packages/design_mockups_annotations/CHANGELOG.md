## 0.1.0

Initial release. `@MockupPreview`, split out of `design_mockups` so that a
widget in an app's `lib/` can declare its own previews without importing a
harness that depends on `flutter_test`.
